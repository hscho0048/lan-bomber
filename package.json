{
  "name": "lan-bomber",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "build:shared": "npm --prefix shared run build",
    "build:server": "npm --prefix server run build",
    "build:client": "npm --prefix client run build",
    "typecheck": "npm --prefix shared run typecheck && npm --prefix server run typecheck && npm --prefix client run typecheck",
    "dev:server": "npm run build:shared && npm run build:client && npm --prefix server run dev",
    "dev:client": "npm run build:shared && npm --prefix client run dev"
  },
  "volta": {
    "node": "20.11.1"
  }
}
